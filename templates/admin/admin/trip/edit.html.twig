{% extends('adminBase.html.twig') %}

{#<title>Edit trip</title>#}

{% block body %}
    <h1>Edit trip</h1>

    {{ include('admin/admin/trip/_form.html.twig', {'button_label': 'Edit'}) }}
    <a class="btn btn-info btn-sm " href="{{ path('admin_trip_index') }}"> <i class="fa fa-arrow-left"> </i>back to list</a>

{% endblock %}
